# Port the Express + Socket.io server listens on
PORT=4000

# Comma-separated list of origins allowed to connect (your frontend dev/prod URLs)
CLIENT_ORIGIN=http://localhost:5173

# Default average consultation time (in minutes) used ONLY until the first
# real consultations happen and the system has actual data to learn from.
DEFAULT_CONSULTATION_MINUTES=5
